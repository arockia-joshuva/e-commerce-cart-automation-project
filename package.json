{
  "name": "amazon-cart-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright automation for the Amazon.in cart assignment",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.1",
    "@types/node": "^24.6.0",
    "dotenv": "^17.2.3",
    "typescript": "^5.9.3"
  }
}
